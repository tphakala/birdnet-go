{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/tphakala/birdnet-go/main/config.schema.json",
  "$ref": "#/$defs/Settings",
  "$defs": {
    "AlertSettings": {
      "properties": {
        "historyRetentionDays": {
          "type": "integer",
          "description": "Days to retain alert history (0 = unlimited)"
        }
      },
      "type": "object",
      "required": [
        "historyRetentionDays"
      ]
    },
    "AllowSubnetBypass": {
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "true to enable subnet bypass"
        },
        "subnet": {
          "type": "string",
          "description": "disable OAuth2 in subnet"
        }
      },
      "type": "object",
      "required": [
        "enabled",
        "subnet"
      ]
    },
    "AudioSettings": {
      "properties": {
        "sources": {
          "items": {
            "$ref": "#/$defs/AudioSourceConfig"
          },
          "type": "array",
          "description": "Audio capture devices"
        },
        "source": {
          "type": "string",
          "description": "Legacy: migrated to Sources on load"
        },
        "ffmpegPath": {
          "type": "string",
          "description": "path to ffmpeg, runtime value"
        },
        "ffmpegVersion": {
          "type": "string",
          "description": "ffmpeg version string, runtime value"
        },
        "ffmpegMajor": {
          "type": "integer",
          "description": "ffmpeg major version number, runtime value"
        },
        "ffmpegMinor": {
          "type": "integer",
          "description": "ffmpeg minor version number, runtime value"
        },
        "soxPath": {
          "type": "string",
          "description": "path to sox, runtime value"
        },
        "streamTransport": {
          "type": "string",
          "description": "preferred transport for audio streaming: \"auto\", \"sse\", or \"ws\""
        },
        "export": {
          "$ref": "#/$defs/ExportSettings",
          "description": "export settings"
        },
        "soundLevel": {
          "$ref": "#/$defs/SoundLevelSettings",
          "description": "sound level monitoring settings"
        },
        "equalizer": {
          "$ref": "#/$defs/EqualizerSettings",
          "description": "equalizer settings (global default)"
        },
        "quietHours": {
          "$ref": "#/$defs/QuietHoursConfig",
          "description": "quiet hours (global default, legacy)"
        },
        "watchdog": {
          "$ref": "#/$defs/WatchdogSettings",
          "description": "liveness watchdog tuning (0 = use default)"
        }
      },
      "type": "object",
      "required": [
        "sources",
        "ffmpegPath",
        "soxPath",
        "streamTransport",
        "export",
        "soundLevel",
        "equalizer",
        "quietHours",
        "watchdog"
      ]
    },
    "AudioSourceConfig": {
      "properties": {
        "name": {
          "type": "string",
          "description": "Required: descriptive name like \"Front Yard Mic\""
        },
        "device": {
          "type": "string",
          "description": "Required: ALSA device ID (e.g., \"sysdefault\", \"hw:0,0\", \"Loopback\")"
        },
        "sampleRate": {
          "type": "integer",
          "description": "Capture rate in Hz (0 = default 48000; set 256000 for bat detectors)"
        },
        "gain": {
          "type": "number",
          "description": "Input gain in dB (0 = no adjustment)"
        },
        "model": {
          "type": "string",
          "description": "AI model: \"\" or \"birdnet\" (default), \"perch_v2\", \"bat\" (future)"
        },
        "models": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "Model IDs for this source (e.g., [\"birdnet\", \"perch_v2\"])"
        },
        "equalizer": {
          "$ref": "#/$defs/EqualizerSettings",
          "description": "Per-source EQ (nil = use global)"
        },
        "quietHours": {
          "$ref": "#/$defs/QuietHoursConfig",
          "description": "Per-source quiet hours"
        }
      },
      "type": "object",
      "required": [
        "name",
        "device",
        "gain",
        "quietHours"
      ]
    },
    "BSGConfig": {
      "properties": {
        "modelPath": {
          "type": "string",
          "description": "path to BSG ONNX model file"
        },
        "labelPath": {
          "type": "string",
          "description": "path to BSG label file"
        },
        "locale": {
          "type": "string",
          "description": "locale for species label translation"
        }
      },
      "type": "object"
    },
    "BackupConfig": {
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "Global flag to enable or disable the entire backup system. If false, no backups (manual or scheduled) will occur."
        },
        "debug": {
          "type": "boolean",
          "description": "If true, enables detailed debug logging for backup operations."
        },
        "encryption": {
          "type": "boolean",
          "description": "If true, enables encryption for backup archives. Requires EncryptionKey to be set."
        },
        "encryptionKey": {
          "type": "string",
          "description": "Base64-encoded encryption key used for AES-256-GCM encryption of backup archives. Must be kept secret and safe."
        },
        "sanitizeConfig": {
          "type": "boolean",
          "description": "If true, sensitive information (like passwords, API keys) will be removed from the configuration file copy that is included in the backup archive."
        },
        "retention": {
          "$ref": "#/$defs/BackupRetention",
          "description": "Defines policies for how long and how many backups are kept."
        },
        "targets": {
          "items": {
            "$ref": "#/$defs/BackupTarget"
          },
          "type": "array",
          "description": "A list of configured backup targets (destinations) where backup archives will be stored."
        },
        "schedules": {
          "items": {
            "$ref": "#/$defs/BackupScheduleConfig"
          },
          "type": "array",
          "description": "A list of schedules (e.g., daily, weekly) that define when automatic backups should run."
        },
        "operationTimeouts": {
          "properties": {
            "backup": {
              "type": "integer"
            },
            "store": {
              "type": "integer"
            },
            "cleanup": {
              "type": "integer"
            },
            "delete": {
              "type": "integer"
            }
          },
          "type": "object",
          "required": [
            "backup",
            "store",
            "cleanup",
            "delete"
          ],
          "description": "OperationTimeouts defines timeouts for various backup operations"
        }
      },
      "type": "object",
      "required": [
        "enabled",
        "debug",
        "encryption",
        "encryptionKey",
        "sanitizeConfig",
        "retention",
        "targets",
        "schedules",
        "operationTimeouts"
      ]
    },
    "BackupRetention": {
      "properties": {
        "maxAge": {
          "type": "string",
          "description": "Duration string for the maximum age of backups to keep (e.g., \"30d\" for 30 days, \"6m\" for 6 months, \"1y\" for 1 year). Backups older than this may be deleted."
        },
        "maxBackups": {
          "type": "integer",
          "description": "Maximum total number of backups to keep for a given source. If 0, no limit by count (only by age or MinBackups)."
        },
        "minBackups": {
          "type": "integer",
          "description": "Minimum number of recent backups to keep for a given source, regardless of their age. This ensures a baseline number of backups are always available."
        }
      },
      "type": "object",
      "required": [
        "maxAge",
        "maxBackups",
        "minBackups"
      ]
    },
    "BackupScheduleConfig": {
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "If true, this specific schedule is active and backups will be attempted at the defined interval. (Valid: true or false)"
        },
        "hour": {
          "type": "integer",
          "description": "The hour of the day when the backup is scheduled to run. (Valid range: 0-23, where 0 is midnight and 23 is 11 PM)"
        },
        "minute": {
          "type": "integer",
          "description": "The minute of the hour when the backup is scheduled to run. (Valid range: 0-59)"
        },
        "weekday": {
          "type": "string",
          "description": "For weekly schedules, the day of the week. Accepts: \"Sunday\", \"Monday\", \"Tuesday\", \"Wednesday\", \"Thursday\", \"Friday\", \"Saturday\" (case-insensitive), or numeric: \"0\" (Sunday) through \"6\" (Saturday). Empty or ignored for daily schedules."
        },
        "isWeekly": {
          "type": "boolean",
          "description": "If true, this schedule is weekly (runs on the specified Weekday at Hour:Minute). If false, it's a daily schedule (runs every day at Hour:Minute). (Valid: true or false)"
        }
      },
      "type": "object",
      "required": [
        "enabled",
        "hour",
        "minute",
        "weekday",
        "isWeekly"
      ]
    },
    "BackupTarget": {
      "properties": {
        "type": {
          "type": "string",
          "description": "Specifies the type of the backup target (e.g., \"local\", \"s3\", \"ftp\", \"sftp\"). This determines the storage mechanism."
        },
        "enabled": {
          "type": "boolean",
          "description": "If true, this backup target will be used for storing backups. At least one target should be enabled for backups to be stored."
        },
        "settings": {
          "type": "object",
          "description": "A map of key-value pairs for target-specific settings. TODO: Consider using BackupTargetSettings interface for type safety after implementing custom YAML unmarshaling."
        }
      },
      "type": "object",
      "required": [
        "type",
        "enabled",
        "settings"
      ]
    },
    "BannerConfig": {
      "properties": {
        "showImage": {
          "type": "boolean",
          "description": "whether to show a custom image"
        },
        "imagePath": {
          "type": "string",
          "description": "relative path or URL to the banner image"
        },
        "title": {
          "type": "string",
          "description": "station name or custom title"
        },
        "description": {
          "type": "string",
          "description": "brief description text"
        },
        "showLocationMap": {
          "type": "boolean",
          "description": "whether to show the location map"
        },
        "showWeather": {
          "type": "boolean",
          "description": "whether to show weather conditions"
        },
        "mapZoom": {
          "type": "integer",
          "description": "zoom level for the location map (default 11)"
        },
        "showPin": {
          "type": "boolean",
          "description": "whether to show the location pin marker (default false)"
        },
        "mapExpandable": {
          "type": "boolean",
          "description": "whether visitors can expand the map (default true)"
        }
      },
      "type": "object",
      "required": [
        "showImage",
        "imagePath",
        "title",
        "description",
        "showLocationMap",
        "showWeather"
      ]
    },
    "BasicAuth": {
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "true to enable password authentication"
        },
        "password": {
          "type": "string",
          "description": "password for admin interface"
        },
        "clientId": {
          "type": "string",
          "description": "client id for OAuth2"
        },
        "clientSecret": {
          "type": "string",
          "description": "client secret for OAuth2"
        },
        "redirectUri": {
          "type": "string",
          "description": "redirect uri for OAuth2"
        },
        "authCodeExp": {
          "type": "integer",
          "description": "duration for authorization code"
        },
        "accessTokenExp": {
          "type": "integer",
          "description": "duration for access token"
        }
      },
      "type": "object",
      "required": [
        "enabled",
        "password",
        "clientId",
        "clientSecret",
        "redirectUri",
        "authCodeExp",
        "accessTokenExp"
      ]
    },
    "BatConfig": {
      "properties": {
        "embeddingModel": {
          "type": "string",
          "description": "path to BirdNET v2.4 embeddings ONNX model"
        },
        "classifierModel": {
          "type": "string",
          "description": "path to bat species classifier ONNX model"
        },
        "labelPath": {
          "type": "string",
          "description": "path to bat species labels file"
        },
        "threshold": {
          "type": "number",
          "description": "confidence threshold for bat detections"
        },
        "locale": {
          "type": "string",
          "description": "locale for species label translation"
        },
        "nighttimeOnly": {
          "type": "boolean",
          "description": "restrict bat detection to nighttime (civil dusk to civil dawn)"
        },
        "falsePositiveFilter": {
          "$ref": "#/$defs/FalsePositiveFilterSettings",
          "description": "false positive filtering for bat detections (level 0-5)"
        },
        "ultrasonicFilter": {
          "$ref": "#/$defs/UltrasonicFilterConfig",
          "description": "post-detection ultrasonic validation filter"
        }
      },
      "type": "object",
      "required": [
        "threshold",
        "nighttimeOnly",
        "falsePositiveFilter",
        "ultrasonicFilter"
      ]
    },
    "BirdNETConfig": {
      "properties": {
        "version": {
          "type": "string",
          "description": "model version: \"2.4\", \"3.0\""
        },
        "debug": {
          "type": "boolean",
          "description": "true to enable debug mode"
        },
        "sensitivity": {
          "type": "number",
          "description": "birdnet analysis sigmoid sensitivity"
        },
        "threshold": {
          "type": "number",
          "description": "threshold for prediction confidence to report"
        },
        "overlap": {
          "type": "number",
          "description": "birdnet analysis overlap between chunks"
        },
        "longitude": {
          "type": "number",
          "description": "longitude of recording location for prediction filtering"
        },
        "latitude": {
          "type": "number",
          "description": "latitude of recording location for prediction filtering"
        },
        "locationConfigured": {
          "type": "boolean",
          "description": "true when location has been explicitly configured by the user"
        },
        "threads": {
          "type": "integer",
          "description": "number of CPU threads to use for analysis"
        },
        "locale": {
          "type": "string",
          "description": "language to use for labels"
        },
        "rangeFilter": {
          "$ref": "#/$defs/RangeFilterSettings",
          "description": "range filter settings"
        },
        "modelPath": {
          "type": "string",
          "description": "path to external model file (empty for embedded)"
        },
        "labelPath": {
          "type": "string",
          "description": "path to external label file (empty for embedded)"
        },
        "useXnnpack": {
          "type": "boolean",
          "description": "true to use XNNPACK delegate for inference acceleration"
        },
        "onnxRuntimePath": {
          "type": "string",
          "description": "path to ONNX Runtime shared library (required for ONNX models)"
        },
        "openVinoPath": {
          "type": "string",
          "description": "path to libopenvino_c shared library (OpenVINO image variants only)"
        },
        "backend": {
          "type": "string",
          "description": "inference backend preference: \"auto\" (default), \"onnx\", or \"openvino\""
        },
        "openVinoDevice": {
          "type": "string",
          "description": "OpenVINO device preference: \"auto\" (default), \"cpu\", or \"gpu\""
        }
      },
      "type": "object",
      "required": [
        "debug",
        "sensitivity",
        "threshold",
        "overlap",
        "longitude",
        "latitude",
        "locationConfigured",
        "threads",
        "locale",
        "rangeFilter",
        "useXnnpack"
      ]
    },
    "BirdweatherSettings": {
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "true to enable birdweather uploads"
        },
        "debug": {
          "type": "boolean",
          "description": "true to enable debug mode"
        },
        "id": {
          "type": "string",
          "description": "birdweather ID"
        },
        "threshold": {
          "type": "number",
          "description": "threshold for prediction confidence for uploads"
        },
        "locationAccuracy": {
          "type": "number",
          "description": "accuracy of location in meters"
        },
        "retrySettings": {
          "$ref": "#/$defs/RetrySettings",
          "description": "settings for retry mechanism"
        }
      },
      "type": "object",
      "required": [
        "enabled",
        "debug",
        "id",
        "threshold",
        "locationAccuracy",
        "retrySettings"
      ]
    },
    "CircuitBreakerConfig": {
      "properties": {
        "enabled": {
          "type": "boolean"
        },
        "max_failures": {
          "type": "integer"
        },
        "timeout": {
          "type": "integer"
        },
        "half_open_max_requests": {
          "type": "integer"
        }
      },
      "type": "object",
      "required": [
        "enabled",
        "max_failures",
        "timeout",
        "half_open_max_requests"
      ]
    },
    "ConsoleOutput": {
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "enable console output"
        },
        "level": {
          "type": "string",
          "description": "log level for console output"
        }
      },
      "type": "object",
      "required": [
        "enabled",
        "level"
      ]
    },
    "CustomColors": {
      "properties": {
        "primary": {
          "type": "string",
          "description": "primary hex color, e.g. \"#2563eb\""
        },
        "accent": {
          "type": "string",
          "description": "accent hex color, e.g. \"#0284c7\""
        }
      },
      "type": "object"
    },
    "DailySummaryConfig": {
      "properties": {
        "summaryLimit": {
          "type": "integer",
          "description": "number of species to show"
        }
      },
      "type": "object",
      "required": [
        "summaryLimit"
      ]
    },
    "Dashboard": {
      "properties": {
        "thumbnails": {
          "$ref": "#/$defs/Thumbnails",
          "description": "thumbnails settings"
        },
        "summaryLimit": {
          "type": "integer",
          "description": "limit for the number of species shown in the summary table"
        },
        "locale": {
          "type": "string",
          "description": "UI locale setting"
        },
        "spectrogram": {
          "$ref": "#/$defs/SpectrogramPreRender",
          "description": "Spectrogram pre-rendering settings"
        },
        "temperatureUnit": {
          "type": "string",
          "description": "display unit for temperature: \"celsius\" or \"fahrenheit\""
        },
        "colorScheme": {
          "type": "string",
          "description": "color scheme: \"blue\", \"forest\", \"amber\", \"violet\", \"rose\", \"custom\""
        },
        "customColors": {
          "$ref": "#/$defs/CustomColors",
          "description": "custom scheme colors (used when colorScheme is \"custom\")"
        },
        "logoStyle": {
          "type": "string",
          "description": "logo display style: \"gradient\" or \"solid\""
        },
        "layout": {
          "$ref": "#/$defs/DashboardLayout",
          "description": "configurable dashboard element layout"
        },
        "defaultAudioGain": {
          "type": "number",
          "description": "Default playback gain in dB (0-24)"
        },
        "liveSpectrogram": {
          "type": "boolean",
          "description": "auto-start live spectrogram on dashboard"
        }
      },
      "type": "object",
      "required": [
        "thumbnails",
        "summaryLimit",
        "spectrogram",
        "temperatureUnit",
        "layout",
        "defaultAudioGain",
        "liveSpectrogram"
      ]
    },
    "DashboardElement": {
      "properties": {
        "id": {
          "type": "string",
          "description": "unique identifier (e.g., \"daily-summary-0\"); generated by migration if empty"
        },
        "type": {
          "type": "string",
          "description": "\"banner\", \"daily-summary\", \"new-species-highlights\", \"currently-hearing\", \"detections-grid\", \"live-spectrogram\", \"video-embed\""
        },
        "enabled": {
          "type": "boolean",
          "description": "whether this element is visible"
        },
        "width": {
          "type": "string",
          "description": "\"full\" or \"half\"; defaults to \"full\" if empty"
        },
        "banner": {
          "$ref": "#/$defs/BannerConfig",
          "description": "config for banner element"
        },
        "video": {
          "$ref": "#/$defs/VideoEmbedConfig",
          "description": "config for video embed element"
        },
        "summary": {
          "$ref": "#/$defs/DailySummaryConfig",
          "description": "config for daily summary element"
        },
        "grid": {
          "$ref": "#/$defs/DetectionsGridConfig",
          "description": "config for detections grid element"
        }
      },
      "type": "object",
      "required": [
        "type",
        "enabled"
      ]
    },
    "DashboardLayout": {
      "properties": {
        "elements": {
          "items": {
            "$ref": "#/$defs/DashboardElement"
          },
          "type": "array"
        }
      },
      "type": "object",
      "required": [
        "elements"
      ]
    },
    "DaylightFilterSettings": {
      "properties": {
        "debug": {
          "type": "boolean",
          "description": "true to enable debug logging"
        },
        "enabled": {
          "type": "boolean",
          "description": "true to enable daylight filter"
        },
        "offset": {
          "type": "integer",
          "description": "hours to adjust daylight window; positive = shrink (lenient), negative = expand (strict)"
        },
        "species": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "species, families, orders, or genera to filter during daylight"
        }
      },
      "type": "object",
      "required": [
        "debug",
        "enabled",
        "offset",
        "species"
      ]
    },
    "DetectionsGridConfig": {
      "properties": {},
      "type": "object"
    },
    "DiskMonitorConfig": {
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "true to enable disk monitoring"
        },
        "paths": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "filesystem paths to monitor"
        }
      },
      "type": "object",
      "required": [
        "enabled",
        "paths"
      ]
    },
    "DogBarkFilterSettings": {
      "properties": {
        "debug": {
          "type": "boolean",
          "description": "true to enable debug mode"
        },
        "enabled": {
          "type": "boolean",
          "description": "true to enable dog bark filter"
        },
        "confidence": {
          "type": "number",
          "description": "confidence threshold for dog bark detection"
        },
        "remember": {
          "type": "integer",
          "description": "how long we should remember bark for filtering?"
        },
        "species": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "species list for filtering"
        }
      },
      "type": "object",
      "required": [
        "debug",
        "enabled",
        "confidence",
        "remember",
        "species"
      ]
    },
    "DynamicThresholdSettings": {
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "true to enable dynamic threshold"
        },
        "debug": {
          "type": "boolean",
          "description": "true to enable debug mode"
        },
        "trigger": {
          "type": "number",
          "description": "trigger threshold for dynamic threshold"
        },
        "min": {
          "type": "number",
          "description": "minimum threshold for dynamic threshold"
        },
        "validHours": {
          "type": "integer",
          "description": "number of hours to consider for dynamic threshold"
        }
      },
      "type": "object",
      "required": [
        "enabled",
        "debug",
        "trigger",
        "min",
        "validHours"
      ]
    },
    "EBirdSettings": {
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "true to enable eBird integration"
        },
        "apiKey": {
          "type": "string",
          "description": "eBird API key"
        },
        "cacheTTL": {
          "type": "integer",
          "description": "cache time-to-live in hours (default: 24)"
        },
        "locale": {
          "type": "string",
          "description": "locale for eBird data (e.g., \"en\", \"es\")"
        }
      },
      "type": "object",
      "required": [
        "enabled",
        "apiKey",
        "cacheTTL",
        "locale"
      ]
    },
    "EqualizerFilter": {
      "properties": {
        "type": {
          "type": "string",
          "description": "e.g., \"LowPass\", \"HighPass\", \"BandPass\", etc."
        },
        "frequency": {
          "type": "number"
        },
        "q": {
          "type": "number"
        },
        "gain": {
          "type": "number",
          "description": "Only used for certain filter types like Peaking"
        },
        "width": {
          "type": "number",
          "description": "Only used for certain filter types like BandPass and BandReject"
        },
        "passes": {
          "type": "integer",
          "description": "Filter passes for added attenuation or gain"
        }
      },
      "type": "object",
      "required": [
        "type",
        "frequency",
        "q",
        "gain",
        "width",
        "passes"
      ]
    },
    "EqualizerSettings": {
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "global flag to enable/disable equalizer filters"
        },
        "filters": {
          "items": {
            "$ref": "#/$defs/EqualizerFilter"
          },
          "type": "array",
          "description": "equalizer filter configuration"
        }
      },
      "type": "object",
      "required": [
        "enabled",
        "filters"
      ]
    },
    "ExportSettings": {
      "properties": {
        "debug": {
          "type": "boolean",
          "description": "true to enable audio export debug"
        },
        "enabled": {
          "type": "boolean",
          "description": "export audio clips containing indentified bird calls"
        },
        "path": {
          "type": "string",
          "description": "path to audio clip export directory"
        },
        "type": {
          "type": "string",
          "description": "audio file type, wav, mp3 or flac"
        },
        "bitrate": {
          "type": "string",
          "description": "bitrate for audio export"
        },
        "retention": {
          "$ref": "#/$defs/RetentionSettings",
          "description": "retention settings"
        },
        "length": {
          "type": "integer",
          "description": "audio capture length in seconds"
        },
        "preCapture": {
          "type": "integer",
          "description": "pre-capture in seconds"
        },
        "gain": {
          "type": "number",
          "description": "gain in dB for audio capture"
        },
        "normalization": {
          "$ref": "#/$defs/NormalizationSettings",
          "description": "audio normalization settings (EBU R128)"
        }
      },
      "type": "object",
      "required": [
        "debug",
        "enabled",
        "path",
        "type",
        "bitrate",
        "retention",
        "length",
        "preCapture",
        "gain",
        "normalization"
      ]
    },
    "ExtendedCaptureSettings": {
      "properties": {
        "enabled": {
          "type": "boolean"
        },
        "maxDuration": {
          "type": "integer"
        },
        "captureBufferSeconds": {
          "type": "integer"
        },
        "species": {
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object",
      "required": [
        "enabled",
        "maxDuration",
        "captureBufferSeconds",
        "species"
      ]
    },
    "FalsePositiveFilterSettings": {
      "properties": {
        "level": {
          "type": "integer",
          "description": "Filtering aggressivity level (0-5): 0=Off, 1=Lenient, 2=Moderate, 3=Balanced, 4=Strict, 5=Maximum"
        }
      },
      "type": "object",
      "required": [
        "level"
      ]
    },
    "FileOutput": {
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "enable file output"
        },
        "path": {
          "type": "string",
          "description": "log file path"
        },
        "max_size": {
          "type": "integer",
          "description": "maximum size in MB before rotation (0 = disabled)"
        },
        "max_age": {
          "type": "integer",
          "description": "maximum age in days to keep rotated logs (0 = no limit)"
        },
        "max_rotated_files": {
          "type": "integer",
          "description": "maximum number of rotated log files to keep (0 = no limit)"
        },
        "compress": {
          "type": "boolean",
          "description": "compress rotated logs with gzip"
        },
        "level": {
          "type": "string",
          "description": "log level for file output"
        }
      },
      "type": "object",
      "required": [
        "enabled",
        "path",
        "max_size",
        "max_age",
        "max_rotated_files",
        "compress",
        "level"
      ]
    },
    "HealthCheckConfig": {
      "properties": {
        "enabled": {
          "type": "boolean"
        },
        "interval": {
          "type": "integer"
        },
        "timeout": {
          "type": "integer"
        }
      },
      "type": "object",
      "required": [
        "enabled",
        "interval",
        "timeout"
      ]
    },
    "HomeAssistantSettings": {
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "true to enable HA auto-discovery"
        },
        "discoveryPrefix": {
          "type": "string",
          "description": "HA discovery topic prefix (default: homeassistant)"
        },
        "deviceName": {
          "type": "string",
          "description": "base name for devices (default: BirdNET-Go)"
        }
      },
      "type": "object",
      "required": [
        "enabled",
        "discoveryPrefix",
        "deviceName"
      ]
    },
    "LiveStreamSettings": {
      "properties": {
        "debug": {
          "type": "boolean",
          "description": "true to enable debug mode"
        },
        "bitRate": {
          "type": "integer",
          "description": "bitrate for live stream in kbps"
        },
        "sampleRate": {
          "type": "integer",
          "description": "sample rate for live stream in Hz"
        },
        "segmentLength": {
          "type": "integer",
          "description": "length of each segment in seconds"
        },
        "ffmpegLogLevel": {
          "type": "string",
          "description": "log level for ffmpeg"
        }
      },
      "type": "object",
      "required": [
        "debug",
        "bitRate",
        "sampleRate",
        "segmentLength",
        "ffmpegLogLevel"
      ]
    },
    "LogDeduplicationSettings": {
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "true to enable log deduplication"
        },
        "healthCheckIntervalSeconds": {
          "type": "integer",
          "description": "Health check interval in seconds (default: 60)"
        }
      },
      "type": "object",
      "required": [
        "enabled",
        "healthCheckIntervalSeconds"
      ]
    },
    "LoggingConfig": {
      "properties": {
        "level": {
          "type": "string",
          "description": "debug, info, warn, error (deprecated, use DefaultLevel)"
        },
        "timezone": {
          "type": "string",
          "description": "\"Local\", \"UTC\", or IANA timezone name like \"Europe/Helsinki\""
        },
        "file": {
          "type": "string",
          "description": "optional log file path (deprecated, use FileOutput)"
        },
        "debug_webhooks": {
          "type": "boolean",
          "description": "if true, logs full webhook details (headers, body, etc.)"
        },
        "default_level": {
          "type": "string",
          "description": "default log level for all modules"
        },
        "console": {
          "$ref": "#/$defs/ConsoleOutput",
          "description": "console output configuration"
        },
        "file_output": {
          "$ref": "#/$defs/FileOutput",
          "description": "file output configuration"
        },
        "modules": {
          "additionalProperties": {
            "$ref": "#/$defs/ModuleOutput"
          },
          "type": "object",
          "description": "per-module output configuration"
        },
        "module_levels": {
          "additionalProperties": {
            "type": "string"
          },
          "type": "object",
          "description": "per-module log levels"
        }
      },
      "type": "object",
      "required": [
        "level",
        "timezone",
        "file",
        "debug_webhooks",
        "default_level",
        "console",
        "file_output",
        "modules",
        "module_levels"
      ]
    },
    "LowMemoryConfig": {
      "properties": {
        "mode": {
          "type": "string",
          "description": "\"auto\" (default), \"on\", \"off\""
        }
      },
      "type": "object",
      "required": [
        "mode"
      ]
    },
    "MQTTSettings": {
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "true to enable MQTT"
        },
        "debug": {
          "type": "boolean",
          "description": "true to enable MQTT debug"
        },
        "broker": {
          "type": "string",
          "description": "MQTT broker URL"
        },
        "topic": {
          "type": "string",
          "description": "MQTT topic"
        },
        "username": {
          "type": "string",
          "description": "MQTT username"
        },
        "password": {
          "type": "string",
          "description": "MQTT password"
        },
        "retain": {
          "type": "boolean",
          "description": "true to retain messages"
        },
        "retrySettings": {
          "$ref": "#/$defs/RetrySettings",
          "description": "settings for retry mechanism"
        },
        "tls": {
          "$ref": "#/$defs/MQTTTLSSettings",
          "description": "TLS/SSL configuration"
        },
        "homeAssistant": {
          "$ref": "#/$defs/HomeAssistantSettings",
          "description": "Home Assistant auto-discovery settings"
        }
      },
      "type": "object",
      "required": [
        "enabled",
        "debug",
        "broker",
        "topic",
        "username",
        "password",
        "retain",
        "retrySettings",
        "tls",
        "homeAssistant"
      ]
    },
    "MQTTTLSSettings": {
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "true to enable TLS (auto-detected from broker URL)"
        },
        "insecureSkipVerify": {
          "type": "boolean",
          "description": "true to skip certificate verification (for self-signed certs)"
        },
        "caCert": {
          "type": "string",
          "description": "path to CA certificate file (managed internally)"
        },
        "clientCert": {
          "type": "string",
          "description": "path to client certificate file (managed internally)"
        },
        "clientKey": {
          "type": "string",
          "description": "path to client key file (managed internally)"
        }
      },
      "type": "object",
      "required": [
        "enabled",
        "insecureSkipVerify"
      ]
    },
    "ModelsConfig": {
      "properties": {
        "enabled": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "list of model IDs to load (e.g., \"birdnet\", \"perch_v2\")"
        },
        "directory": {
          "type": "string",
          "description": "base directory for downloaded model files"
        },
        "installed": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "list of installed model IDs managed by the model gallery"
        }
      },
      "type": "object",
      "required": [
        "enabled"
      ]
    },
    "ModuleOutput": {
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "enable module-specific output"
        },
        "file_path": {
          "type": "string",
          "description": "dedicated file path for this module"
        },
        "level": {
          "type": "string",
          "description": "log level override for this module"
        },
        "console_also": {
          "type": "boolean",
          "description": "also log to console"
        },
        "max_size": {
          "type": "integer",
          "description": "maximum size in MB before rotation (0 = use FileOutput default)"
        },
        "max_age": {
          "type": "integer",
          "description": "maximum age in days (0 = use FileOutput default)"
        },
        "max_rotated_files": {
          "type": "integer",
          "description": "maximum number of rotated files (0 = use FileOutput default)"
        },
        "compress": {
          "type": "boolean",
          "description": "compress rotated logs (nil = use FileOutput default)"
        }
      },
      "type": "object",
      "required": [
        "enabled",
        "file_path",
        "level",
        "console_also",
        "max_size",
        "max_age",
        "max_rotated_files"
      ]
    },
    "MonitoringSettings": {
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "true to enable system resource monitoring"
        },
        "checkInterval": {
          "type": "integer",
          "description": "interval in seconds between resource checks"
        },
        "cpu": {
          "$ref": "#/$defs/ResourceEnabled",
          "description": "CPU metric collection"
        },
        "memory": {
          "$ref": "#/$defs/ResourceEnabled",
          "description": "Memory metric collection"
        },
        "disk": {
          "$ref": "#/$defs/DiskMonitorConfig",
          "description": "Disk metric collection"
        }
      },
      "type": "object",
      "required": [
        "enabled",
        "checkInterval",
        "cpu",
        "memory",
        "disk"
      ]
    },
    "NewSpeciesTemplate": {
      "properties": {
        "title": {
          "type": "string"
        },
        "message": {
          "type": "string"
        }
      },
      "type": "object",
      "required": [
        "title",
        "message"
      ]
    },
    "NormalizationSettings": {
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "true to enable loudness normalization"
        },
        "targetLUFS": {
          "type": "number",
          "description": "target integrated loudness in LUFS (default: -23)"
        },
        "loudnessRange": {
          "type": "number",
          "description": "loudness range in LU (default: 7)"
        },
        "truePeak": {
          "type": "number",
          "description": "true peak limit in dBTP (default: -2)"
        }
      },
      "type": "object",
      "required": [
        "enabled",
        "targetLUFS",
        "loudnessRange",
        "truePeak"
      ]
    },
    "NotificationConfig": {
      "properties": {
        "push": {
          "$ref": "#/$defs/PushSettings"
        },
        "templates": {
          "$ref": "#/$defs/NotificationTemplates"
        }
      },
      "type": "object",
      "required": [
        "push",
        "templates"
      ]
    },
    "NotificationTemplates": {
      "properties": {
        "newSpecies": {
          "$ref": "#/$defs/NewSpeciesTemplate"
        }
      },
      "type": "object",
      "required": [
        "newSpecies"
      ]
    },
    "OAuthProviderConfig": {
      "properties": {
        "provider": {
          "type": "string",
          "description": "Provider ID: \"google\", \"github\", \"microsoft\""
        },
        "enabled": {
          "type": "boolean",
          "description": "true to enable this provider"
        },
        "clientId": {
          "type": "string",
          "description": "OAuth2 client ID"
        },
        "clientSecret": {
          "type": "string",
          "description": "OAuth2 client secret"
        },
        "redirectUri": {
          "type": "string",
          "description": "OAuth2 redirect URI (optional, auto-generated if empty)"
        },
        "userId": {
          "type": "string",
          "description": "Allowed user ID/email for this provider"
        },
        "issuerUrl": {
          "type": "string",
          "description": "OIDC issuer/discovery URL (required when provider is \"oidc\")"
        },
        "scopes": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "Custom OAuth scopes (OIDC default: openid, profile, email)"
        }
      },
      "type": "object",
      "required": [
        "provider",
        "enabled",
        "clientId",
        "clientSecret",
        "redirectUri",
        "userId",
        "issuerUrl"
      ]
    },
    "OpenWeatherSettings": {
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "true to enable OpenWeather integration, for legacy support"
        },
        "apiKey": {
          "type": "string",
          "description": "OpenWeather API key"
        },
        "endpoint": {
          "type": "string",
          "description": "OpenWeather API endpoint"
        },
        "units": {
          "type": "string",
          "description": "units of measurement: standard, metric, or imperial"
        },
        "language": {
          "type": "string",
          "description": "language code for the response"
        }
      },
      "type": "object",
      "required": [
        "enabled",
        "apiKey",
        "endpoint",
        "units",
        "language"
      ]
    },
    "PerchConfig": {
      "properties": {
        "modelPath": {
          "type": "string",
          "description": "path to Perch v2 ONNX model file"
        },
        "labelPath": {
          "type": "string",
          "description": "path to Perch v2 label CSV file"
        },
        "threshold": {
          "type": "number",
          "description": "confidence threshold for detections"
        },
        "locale": {
          "type": "string",
          "description": "locale for species label translation"
        }
      },
      "type": "object",
      "required": [
        "threshold"
      ]
    },
    "PrivacyFilterSettings": {
      "properties": {
        "debug": {
          "type": "boolean",
          "description": "true to enable debug mode"
        },
        "enabled": {
          "type": "boolean",
          "description": "true to enable privacy filter"
        },
        "confidence": {
          "type": "number",
          "description": "confidence threshold for human detection"
        }
      },
      "type": "object",
      "required": [
        "debug",
        "enabled",
        "confidence"
      ]
    },
    "PublicAccess": {
      "properties": {
        "liveAudio": {
          "type": "boolean",
          "description": "allow unauthenticated users to start/listen to live audio streams"
        }
      },
      "type": "object",
      "required": [
        "liveAudio"
      ]
    },
    "PushFilterConfig": {
      "properties": {
        "types": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "priorities": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "components": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "metadata_filters": {
          "type": "object"
        }
      },
      "type": "object",
      "required": [
        "types",
        "priorities",
        "components",
        "metadata_filters"
      ]
    },
    "PushProviderConfig": {
      "properties": {
        "type": {
          "type": "string"
        },
        "enabled": {
          "type": "boolean"
        },
        "name": {
          "type": "string"
        },
        "filter": {
          "$ref": "#/$defs/PushFilterConfig"
        },
        "urls": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "Shoutrrr-specific"
        },
        "timeout": {
          "type": "integer"
        },
        "command": {
          "type": "string",
          "description": "Script-specific"
        },
        "args": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "environment": {
          "additionalProperties": {
            "type": "string"
          },
          "type": "object"
        },
        "input_format": {
          "type": "string"
        },
        "endpoints": {
          "items": {
            "$ref": "#/$defs/WebhookEndpointConfig"
          },
          "type": "array",
          "description": "Webhook-specific"
        },
        "template": {
          "type": "string",
          "description": "Custom JSON template"
        }
      },
      "type": "object",
      "required": [
        "type",
        "enabled",
        "name",
        "filter",
        "urls",
        "timeout",
        "command",
        "args",
        "environment",
        "input_format",
        "endpoints",
        "template"
      ]
    },
    "PushSettings": {
      "properties": {
        "enabled": {
          "type": "boolean"
        },
        "default_timeout": {
          "type": "integer"
        },
        "max_retries": {
          "type": "integer"
        },
        "retry_delay": {
          "type": "integer"
        },
        "circuit_breaker": {
          "$ref": "#/$defs/CircuitBreakerConfig"
        },
        "health_check": {
          "$ref": "#/$defs/HealthCheckConfig"
        },
        "rate_limiting": {
          "$ref": "#/$defs/RateLimitingConfig"
        },
        "providers": {
          "items": {
            "$ref": "#/$defs/PushProviderConfig"
          },
          "type": "array"
        },
        "minConfidenceThreshold": {
          "type": "number",
          "description": "0.0-1.0, 0 = disabled"
        },
        "speciesCooldownMinutes": {
          "type": "integer",
          "description": "0 = disabled"
        }
      },
      "type": "object",
      "required": [
        "enabled",
        "default_timeout",
        "max_retries",
        "retry_delay",
        "circuit_breaker",
        "health_check",
        "rate_limiting",
        "providers",
        "minConfidenceThreshold",
        "speciesCooldownMinutes"
      ]
    },
    "QuietHoursConfig": {
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "true to enable quiet hours"
        },
        "mode": {
          "type": "string",
          "description": "\"fixed\" or \"solar\""
        },
        "startTime": {
          "type": "string",
          "description": "\"HH:MM\" format for fixed mode (e.g., \"22:00\")"
        },
        "endTime": {
          "type": "string",
          "description": "\"HH:MM\" format for fixed mode (e.g., \"06:00\")"
        },
        "startEvent": {
          "type": "string",
          "description": "\"sunset\" or \"sunrise\" for solar mode"
        },
        "startOffset": {
          "type": "integer",
          "description": "minutes relative to start event (positive = after, negative = before)"
        },
        "endEvent": {
          "type": "string",
          "description": "\"sunset\" or \"sunrise\" for solar mode"
        },
        "endOffset": {
          "type": "integer",
          "description": "minutes relative to end event (positive = after, negative = before)"
        }
      },
      "type": "object",
      "required": [
        "enabled",
        "mode",
        "startTime",
        "endTime",
        "startEvent",
        "startOffset",
        "endEvent",
        "endOffset"
      ]
    },
    "RTSPHealthSettings": {
      "properties": {
        "healthyDataThreshold": {
          "type": "integer",
          "description": "seconds before stream considered unhealthy (default: 60)"
        },
        "monitoringInterval": {
          "type": "integer",
          "description": "health check interval in seconds (default: 30)"
        }
      },
      "type": "object",
      "required": [
        "healthyDataThreshold",
        "monitoringInterval"
      ]
    },
    "RTSPSettings": {
      "properties": {
        "streams": {
          "items": {
            "$ref": "#/$defs/StreamConfig"
          },
          "type": "array",
          "description": "Stream configurations"
        },
        "urls": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "Legacy: accepts old format, migrated on load"
        },
        "transport": {
          "type": "string",
          "description": "Legacy: global default, migrated on load"
        },
        "health": {
          "$ref": "#/$defs/RTSPHealthSettings",
          "description": "Health monitoring settings"
        },
        "ffmpegParameters": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "Custom FFmpeg parameters"
        }
      },
      "type": "object",
      "required": [
        "streams",
        "health",
        "ffmpegParameters"
      ]
    },
    "RangeFilterSettings": {
      "properties": {
        "debug": {
          "type": "boolean",
          "description": "true to enable debug mode"
        },
        "model": {
          "type": "string",
          "description": "range filter model version: \"legacy\" for v1, \"v3\" for geomodel v3.0, or empty/default for v2"
        },
        "modelPath": {
          "type": "string",
          "description": "path to external meta model file (empty for embedded)"
        },
        "labelsPath": {
          "type": "string",
          "description": "path to geomodel labels file (required when geomodel differs from classifier labels)"
        },
        "threshold": {
          "type": "number",
          "description": "rangefilter species occurrence threshold"
        },
        "passUnmappedSpecies": {
          "type": "boolean",
          "description": "true to pass through species absent from geomodel (score 1.0); false to filter them out (score 0.0)"
        },
        "species": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "list of included species, runtime value"
        },
        "lastUpdated": {
          "type": "string",
          "format": "date-time",
          "description": "last time the species list was updated, runtime value"
        }
      },
      "type": "object",
      "required": [
        "debug",
        "model",
        "modelPath",
        "threshold",
        "passUnmappedSpecies",
        "lastUpdated"
      ]
    },
    "RateLimitingConfig": {
      "properties": {
        "enabled": {
          "type": "boolean"
        },
        "requests_per_minute": {
          "type": "integer"
        },
        "burst_size": {
          "type": "integer"
        }
      },
      "type": "object",
      "required": [
        "enabled",
        "requests_per_minute",
        "burst_size"
      ]
    },
    "RealtimeSettings": {
      "properties": {
        "interval": {
          "type": "integer",
          "description": "minimum interval between log messages in seconds"
        },
        "processingTime": {
          "type": "boolean",
          "description": "true to report processing time for each prediction"
        },
        "audio": {
          "$ref": "#/$defs/AudioSettings",
          "description": "Audio processing settings"
        },
        "dashboard": {
          "$ref": "#/$defs/Dashboard",
          "description": "Dashboard settings"
        },
        "dynamicThreshold": {
          "$ref": "#/$defs/DynamicThresholdSettings",
          "description": "Dynamic threshold settings"
        },
        "falsePositiveFilter": {
          "$ref": "#/$defs/FalsePositiveFilterSettings",
          "description": "False positive filtering aggressivity settings"
        },
        "log": {
          "properties": {
            "enabled": {
              "type": "boolean"
            },
            "path": {
              "type": "string"
            }
          },
          "type": "object",
          "required": [
            "enabled",
            "path"
          ]
        },
        "logDeduplication": {
          "$ref": "#/$defs/LogDeduplicationSettings",
          "description": "Log deduplication settings"
        },
        "birdweather": {
          "$ref": "#/$defs/BirdweatherSettings",
          "description": "Birdweather integration settings"
        },
        "ebird": {
          "$ref": "#/$defs/EBirdSettings",
          "description": "eBird integration settings"
        },
        "privacyFilter": {
          "$ref": "#/$defs/PrivacyFilterSettings",
          "description": "Privacy filter settings"
        },
        "dogBarkFilter": {
          "$ref": "#/$defs/DogBarkFilterSettings",
          "description": "Dog bark filter settings"
        },
        "daylightFilter": {
          "$ref": "#/$defs/DaylightFilterSettings",
          "description": "Daylight filter settings"
        },
        "rtsp": {
          "$ref": "#/$defs/RTSPSettings",
          "description": "RTSP settings"
        },
        "mqtt": {
          "$ref": "#/$defs/MQTTSettings",
          "description": "MQTT settings"
        },
        "telemetry": {
          "$ref": "#/$defs/TelemetrySettings",
          "description": "Telemetry settings"
        },
        "monitoring": {
          "$ref": "#/$defs/MonitoringSettings",
          "description": "System resource monitoring settings"
        },
        "species": {
          "$ref": "#/$defs/SpeciesSettings",
          "description": "Custom thresholds and actions for species"
        },
        "weather": {
          "$ref": "#/$defs/WeatherSettings",
          "description": "Weather provider related settings"
        },
        "speciesTracking": {
          "$ref": "#/$defs/SpeciesTrackingSettings",
          "description": "New species tracking settings"
        },
        "extendedCapture": {
          "$ref": "#/$defs/ExtendedCaptureSettings",
          "description": "Extended capture for long calling species"
        }
      },
      "type": "object",
      "required": [
        "interval",
        "processingTime",
        "audio",
        "dashboard",
        "dynamicThreshold",
        "falsePositiveFilter",
        "log",
        "logDeduplication",
        "birdweather",
        "ebird",
        "privacyFilter",
        "dogBarkFilter",
        "daylightFilter",
        "rtsp",
        "mqtt",
        "telemetry",
        "monitoring",
        "species",
        "weather",
        "speciesTracking",
        "extendedCapture"
      ]
    },
    "ResourceEnabled": {
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "true to enable monitoring for this resource"
        }
      },
      "type": "object",
      "required": [
        "enabled"
      ]
    },
    "RetentionSettings": {
      "properties": {
        "debug": {
          "type": "boolean",
          "description": "true to enable retention debug"
        },
        "policy": {
          "type": "string",
          "description": "retention policy, \"none\", \"age\" or \"usage\""
        },
        "maxAge": {
          "type": "string",
          "description": "maximum age of audio clips to keep"
        },
        "maxUsage": {
          "type": "string",
          "description": "maximum disk usage percentage before cleanup"
        },
        "minClips": {
          "type": "integer",
          "description": "minimum number of clips per species to keep"
        },
        "keepSpectrograms": {
          "type": "boolean",
          "description": "true to keep spectrograms"
        },
        "checkInterval": {
          "type": "integer",
          "description": "cleanup check interval in minutes (default: 15)"
        }
      },
      "type": "object",
      "required": [
        "debug",
        "policy",
        "maxAge",
        "maxUsage",
        "minClips",
        "keepSpectrograms",
        "checkInterval"
      ]
    },
    "RetrySettings": {
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "true to enable retry mechanism"
        },
        "maxRetries": {
          "type": "integer",
          "description": "maximum number of retry attempts"
        },
        "initialDelay": {
          "type": "integer",
          "description": "initial delay before first retry in seconds"
        },
        "maxDelay": {
          "type": "integer",
          "description": "maximum delay between retries in seconds"
        },
        "backoffMultiplier": {
          "type": "number",
          "description": "multiplier for exponential backoff"
        }
      },
      "type": "object",
      "required": [
        "enabled",
        "maxRetries",
        "initialDelay",
        "maxDelay",
        "backoffMultiplier"
      ]
    },
    "Season": {
      "properties": {
        "startMonth": {
          "type": "integer",
          "description": "Month when season starts (1-12)"
        },
        "startDay": {
          "type": "integer",
          "description": "Day when season starts (1-31)"
        }
      },
      "type": "object",
      "required": [
        "startMonth",
        "startDay"
      ]
    },
    "SeasonalTrackingSettings": {
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "true to enable seasonal tracking"
        },
        "windowDays": {
          "type": "integer",
          "description": "Days to show \"new this season\" indicator (default: 21)"
        },
        "seasons": {
          "additionalProperties": {
            "$ref": "#/$defs/Season"
          },
          "type": "object",
          "description": "Season definitions"
        }
      },
      "type": "object",
      "required": [
        "enabled",
        "windowDays",
        "seasons"
      ]
    },
    "Security": {
      "properties": {
        "debug": {
          "type": "boolean",
          "description": "true to enable debug mode"
        },
        "baseUrl": {
          "type": "string",
          "description": "BaseURL is the complete external URL for this instance, including\nscheme, host, and optional port (e.g., \"https://birdnet.example.com:5500\").\nUsed for generating OAuth redirect URLs and notification links.\nTakes precedence over Host when set.\nCan be overridden with BIRDNET_URL environment variable.\nNOTE: This field is prepared for future implementation (issue #1462)"
        },
        "host": {
          "type": "string",
          "description": "Host is the primary hostname used for TLS certificates,\nOAuth redirect URLs, and notification link generation.\nRequired when using AutoTLS or authentication providers.\nAlso used to generate URLs in push notifications - set this\nto your public hostname when using a reverse proxy.\nCan be overridden with BIRDNET_HOST environment variable."
        },
        "autoTls": {
          "type": "boolean"
        },
        "tlsMode": {
          "type": "string",
          "description": "TLSMode controls TLS certificate management. Valid values:\n  \"\" (none)      - TLS disabled\n  \"autotls\"      - automatic via Let's Encrypt\n  \"manual\"       - user-provided certificates\n  \"selfsigned\"   - auto-generated self-signed certificates"
        },
        "selfSignedValidity": {
          "type": "string",
          "description": "SelfSignedValidity is the validity duration for self-signed certificates.\nUses Go duration format with day/month suffixes (e.g., \"365d\", \"1y\")."
        },
        "tlsPort": {
          "type": "string",
          "description": "port for HTTPS (default: 8443)"
        },
        "redirectToHttps": {
          "type": "boolean",
          "description": "true to redirect to HTTPS"
        },
        "allowSubnetBypass": {
          "$ref": "#/$defs/AllowSubnetBypass",
          "description": "subnet bypass configuration"
        },
        "trustedProxies": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "TrustedProxies lists reverse proxies (CIDR ranges or bare IPs) whose\nforwarded client-IP headers (CF-Connecting-IP, X-Forwarded-For, X-Real-IP)\nmay be trusted. Loopback, link-local, and private (RFC1918/ULA) peers are always\ntrusted in addition to these, so a default port-forwarded install and the\ncommon local cloudflared topology work without configuration. When the\nimmediate peer is not trusted, forwarded headers are ignored and the real\nconnection address is used, preventing source-IP spoofing on a directly\nexposed instance. The reserved value \"cloudflare\" (TrustedProxyCloudflarePreset)\nexpands to Cloudflare's published edge ranges. Hot-reloadable."
        },
        "publicAccess": {
          "$ref": "#/$defs/PublicAccess",
          "description": "features accessible without authentication"
        },
        "privateMode": {
          "type": "boolean",
          "description": "PrivateMode, when true, requires the user to authenticate before any\nUI data is shown. Enforcement lives at the v2 API data layer, which\nreturns 401 to unauthenticated requests; the public SPA shell is still\nserved so it can render a login form instead of the dashboard,\ndetections, analytics, search, about, and notifications views. Settings\nand system routes are additionally auth-gated at the HTTP layer.\nPublicAccess.LiveAudio still applies independently.\nDefault is false to preserve guest-friendly upstream behavior."
        },
        "basicAuth": {
          "$ref": "#/$defs/BasicAuth",
          "description": "password authentication configuration"
        },
        "oauthProviders": {
          "items": {
            "$ref": "#/$defs/OAuthProviderConfig"
          },
          "type": "array",
          "description": "OAuthProviders is the new array-based OAuth configuration.\nThis is the preferred format for configuring OAuth providers."
        },
        "googleAuth": {
          "$ref": "#/$defs/SocialProvider"
        },
        "githubAuth": {
          "$ref": "#/$defs/SocialProvider"
        },
        "microsoftAuth": {
          "$ref": "#/$defs/SocialProvider"
        },
        "sessionSecret": {
          "type": "string",
          "description": "secret for session cookie"
        },
        "sessionDuration": {
          "type": "integer",
          "description": "duration for browser session cookies"
        }
      },
      "type": "object",
      "required": [
        "debug",
        "baseUrl",
        "host",
        "tlsMode",
        "selfSignedValidity",
        "tlsPort",
        "redirectToHttps",
        "allowSubnetBypass",
        "trustedProxies",
        "publicAccess",
        "privateMode",
        "basicAuth",
        "oauthProviders",
        "sessionSecret",
        "sessionDuration"
      ]
    },
    "SentrySettings": {
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "true to enable Sentry error tracking (opt-in)"
        },
        "debug": {
          "type": "boolean",
          "description": "true to enable transparent telemetry logging"
        }
      },
      "type": "object",
      "required": [
        "enabled",
        "debug"
      ]
    },
    "Settings": {
      "properties": {
        "debug": {
          "type": "boolean",
          "description": "true to enable debug mode"
        },
        "version": {
          "type": "string",
          "description": "Version from build"
        },
        "buildDate": {
          "type": "string",
          "description": "Build date from build"
        },
        "systemId": {
          "type": "string",
          "description": "Unique system identifier for telemetry"
        },
        "validationWarnings": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "Configuration validation warnings for telemetry"
        },
        "logging": {
          "$ref": "#/$defs/LoggingConfig",
          "description": "centralized logging configuration"
        },
        "main": {
          "properties": {
            "name": {
              "type": "string"
            },
            "timeAs24h": {
              "type": "boolean"
            }
          },
          "type": "object",
          "required": [
            "name",
            "timeAs24h"
          ]
        },
        "birdnet": {
          "$ref": "#/$defs/BirdNETConfig",
          "description": "BirdNET configuration"
        },
        "perch": {
          "$ref": "#/$defs/PerchConfig",
          "description": "Perch v2 model configuration"
        },
        "bat": {
          "$ref": "#/$defs/BatConfig",
          "description": "Bat detection configuration"
        },
        "bsg": {
          "$ref": "#/$defs/BSGConfig",
          "description": "BSG regional bird model configuration"
        },
        "models": {
          "$ref": "#/$defs/ModelsConfig",
          "description": "Global model enablement and management"
        },
        "lowMemory": {
          "$ref": "#/$defs/LowMemoryConfig",
          "description": "Low-memory mode override (auto/on/off) for constrained systems"
        },
        "taxonomySynonyms": {
          "additionalProperties": {
            "type": "string"
          },
          "type": "object",
          "description": "Optional scientific-name synonym overrides merged with built-ins"
        },
        "realtime": {
          "$ref": "#/$defs/RealtimeSettings",
          "description": "Realtime processing settings"
        },
        "webServer": {
          "$ref": "#/$defs/WebServerSettings",
          "description": "web server configuration"
        },
        "security": {
          "$ref": "#/$defs/Security",
          "description": "security configuration"
        },
        "sentry": {
          "$ref": "#/$defs/SentrySettings",
          "description": "Sentry error tracking configuration"
        },
        "output": {
          "properties": {
            "file": {
              "properties": {},
              "type": "object"
            },
            "sqlite": {
              "properties": {
                "enabled": {
                  "type": "boolean"
                },
                "path": {
                  "type": "string"
                }
              },
              "type": "object",
              "required": [
                "enabled",
                "path"
              ]
            },
            "mysql": {
              "properties": {
                "enabled": {
                  "type": "boolean"
                },
                "username": {
                  "type": "string"
                },
                "password": {
                  "type": "string"
                },
                "database": {
                  "type": "string"
                },
                "host": {
                  "type": "string"
                },
                "port": {
                  "type": "string"
                }
              },
              "type": "object",
              "required": [
                "enabled",
                "username",
                "password",
                "database",
                "host",
                "port"
              ]
            }
          },
          "type": "object",
          "required": [
            "file",
            "sqlite",
            "mysql"
          ]
        },
        "backup": {
          "$ref": "#/$defs/BackupConfig",
          "description": "Backup configuration"
        },
        "notification": {
          "$ref": "#/$defs/NotificationConfig",
          "description": "Configuration for push notifications"
        },
        "alerting": {
          "$ref": "#/$defs/AlertSettings",
          "description": "Alerting rules engine settings"
        }
      },
      "type": "object",
      "required": [
        "debug",
        "logging",
        "main",
        "birdnet",
        "perch",
        "bat",
        "bsg",
        "models",
        "lowMemory",
        "taxonomySynonyms",
        "realtime",
        "webServer",
        "security",
        "sentry",
        "output",
        "backup",
        "notification",
        "alerting"
      ],
      "title": "BirdNET-Go Configuration",
      "description": "Top-level configuration for BirdNET-Go."
    },
    "SocialProvider": {
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "true to enable social provider"
        },
        "clientId": {
          "type": "string",
          "description": "client id for OAuth2"
        },
        "clientSecret": {
          "type": "string",
          "description": "client secret for OAuth2"
        },
        "redirectUri": {
          "type": "string",
          "description": "redirect uri for OAuth2"
        },
        "userId": {
          "type": "string",
          "description": "valid user id for OAuth2"
        }
      },
      "type": "object",
      "required": [
        "enabled",
        "clientId",
        "clientSecret",
        "redirectUri",
        "userId"
      ]
    },
    "SoundLevelSettings": {
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "true to enable sound level monitoring"
        },
        "interval": {
          "type": "integer",
          "description": "measurement interval in seconds (default: 10)"
        },
        "debug": {
          "type": "boolean",
          "description": "true to enable debug logging for sound level monitoring"
        },
        "debugRealtimeLogging": {
          "type": "boolean",
          "description": "true to log debug messages for every realtime update, false to log only at configured interval"
        }
      },
      "type": "object",
      "required": [
        "enabled",
        "interval",
        "debug",
        "debugRealtimeLogging"
      ]
    },
    "SpeciesAction": {
      "properties": {
        "type": {
          "type": "string",
          "description": "Type of action (ExecuteCommand, etc)"
        },
        "command": {
          "type": "string",
          "description": "Path to the command to execute"
        },
        "parameters": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "Action parameters"
        },
        "executeDefaults": {
          "type": "boolean",
          "description": "Whether to also execute default actions"
        }
      },
      "type": "object",
      "required": [
        "type",
        "command",
        "parameters",
        "executeDefaults"
      ]
    },
    "SpeciesConfig": {
      "properties": {
        "threshold": {
          "type": "number",
          "description": "Confidence threshold"
        },
        "interval": {
          "type": "integer",
          "description": "Custom interval in seconds (0 = use default)"
        },
        "actions": {
          "items": {
            "$ref": "#/$defs/SpeciesAction"
          },
          "type": "array",
          "description": "List of actions to execute"
        }
      },
      "type": "object",
      "required": [
        "threshold",
        "interval",
        "actions"
      ]
    },
    "SpeciesSettings": {
      "properties": {
        "include": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "Always include these species"
        },
        "exclude": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "Always exclude these species"
        },
        "config": {
          "additionalProperties": {
            "$ref": "#/$defs/SpeciesConfig"
          },
          "type": "object",
          "description": "Per-species configuration (keys normalized to lowercase)"
        }
      },
      "type": "object",
      "required": [
        "include",
        "exclude",
        "config"
      ]
    },
    "SpeciesTrackingSettings": {
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "true to enable new species tracking"
        },
        "newSpeciesWindowDays": {
          "type": "integer",
          "description": "Days to consider a species \"new\" (default: 14)"
        },
        "syncIntervalMinutes": {
          "type": "integer",
          "description": "Interval to sync with database (default: 60)"
        },
        "notificationSuppressionHours": {
          "type": "integer",
          "description": "Hours to suppress duplicate notifications (default: 168)"
        },
        "yearlyTracking": {
          "$ref": "#/$defs/YearlyTrackingSettings",
          "description": "Settings for yearly species tracking"
        },
        "seasonalTracking": {
          "$ref": "#/$defs/SeasonalTrackingSettings",
          "description": "Settings for seasonal species tracking"
        }
      },
      "type": "object",
      "required": [
        "enabled",
        "newSpeciesWindowDays",
        "syncIntervalMinutes",
        "notificationSuppressionHours",
        "yearlyTracking",
        "seasonalTracking"
      ]
    },
    "SpectrogramPreRender": {
      "properties": {
        "mode": {
          "type": "string",
          "description": "Generation mode: \"auto\" (default), \"prerender\", \"user-requested\""
        },
        "enabled": {
          "type": "boolean",
          "description": "DEPRECATED: Use Mode instead. Kept for backward compatibility (true = \"prerender\", false = \"auto\")"
        },
        "size": {
          "type": "string",
          "description": "Default size for all modes (see recommendations below)"
        },
        "raw": {
          "type": "boolean",
          "description": "Generate raw spectrogram without axes/legend (default: true)"
        },
        "style": {
          "type": "string",
          "description": "Visual style preset: \"default\", \"scientific_dark\", \"high_contrast_dark\", \"scientific\""
        },
        "dynamicRange": {
          "type": "string",
          "description": "Dynamic range in dB: \"80\" (high contrast), \"100\" (standard), \"120\" (extended)"
        }
      },
      "type": "object",
      "required": [
        "mode",
        "enabled",
        "size",
        "raw",
        "style",
        "dynamicRange"
      ]
    },
    "StreamConfig": {
      "properties": {
        "name": {
          "type": "string",
          "description": "Required: descriptive name like \"Front Yard\""
        },
        "url": {
          "type": "string",
          "description": "Required: stream URL"
        },
        "enabled": {
          "type": "boolean",
          "description": "true when the configured stream should be active"
        },
        "type": {
          "type": "string",
          "description": "Stream type: rtsp, http, hls, rtmp, udp"
        },
        "transport": {
          "type": "string",
          "description": "Transport: tcp or udp (for RTSP/RTMP)"
        },
        "channelMode": {
          "type": "string",
          "description": "Channel handling: downmix, left, or right"
        },
        "equalizer": {
          "$ref": "#/$defs/EqualizerSettings",
          "description": "Per-stream EQ (nil = use global)"
        },
        "quietHours": {
          "$ref": "#/$defs/QuietHoursConfig",
          "description": "Quiet hours configuration"
        },
        "models": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "Model IDs for this stream (e.g., [\"birdnet\", \"perch_v2\"])"
        }
      },
      "type": "object",
      "required": [
        "name",
        "url",
        "enabled",
        "type",
        "transport",
        "channelMode",
        "quietHours"
      ]
    },
    "TelemetrySettings": {
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "true to enable Prometheus compatible telemetry endpoint"
        },
        "listen": {
          "type": "string",
          "description": "IP address and port to listen on"
        }
      },
      "type": "object",
      "required": [
        "enabled",
        "listen"
      ]
    },
    "Thumbnails": {
      "properties": {
        "debug": {
          "type": "boolean",
          "description": "true to enable debug mode"
        },
        "summary": {
          "type": "boolean",
          "description": "show thumbnails on summary table"
        },
        "recent": {
          "type": "boolean",
          "description": "show thumbnails on recent table"
        },
        "imageProvider": {
          "type": "string",
          "description": "preferred image provider: \"auto\", \"wikimedia\", \"avicommons\""
        },
        "fallbackPolicy": {
          "type": "string",
          "description": "fallback policy: \"none\", \"all\" - try all available providers if preferred fails"
        }
      },
      "type": "object",
      "required": [
        "debug",
        "summary",
        "recent",
        "imageProvider",
        "fallbackPolicy"
      ]
    },
    "UltrasonicFilterConfig": {
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "enable ultrasonic validation filter"
        },
        "cvThreshold": {
          "type": "number",
          "description": "detections with US frame CV below this are tagged unlikely"
        },
        "fftSize": {
          "type": "integer",
          "description": "FFT window size in samples (must be power of 2)"
        },
        "hopSize": {
          "type": "integer",
          "description": "STFT hop size in samples"
        },
        "frequencySplitHz": {
          "type": "integer",
          "description": "boundary between audible and ultrasonic bands in Hz"
        }
      },
      "type": "object",
      "required": [
        "enabled",
        "cvThreshold",
        "fftSize",
        "hopSize",
        "frequencySplitHz"
      ]
    },
    "VideoEmbedConfig": {
      "properties": {
        "url": {
          "type": "string",
          "description": "YouTube URL or video ID"
        },
        "title": {
          "type": "string",
          "description": "optional display title"
        }
      },
      "type": "object",
      "required": [
        "url",
        "title"
      ]
    },
    "WatchdogSettings": {
      "properties": {
        "checkInterval": {
          "type": "integer",
          "description": "tick period (default 10s)"
        },
        "silenceThreshold": {
          "type": "integer",
          "description": "silence before alarm (default 30s)"
        },
        "maxRetries": {
          "type": "integer",
          "description": "restart attempts before escalation (default 3)"
        },
        "retryBackoff": {
          "type": "integer",
          "description": "wait between retries (default 5s)"
        },
        "cooldown": {
          "type": "integer",
          "description": "alarm suppression after recovery (default 60s)"
        },
        "escalationTimeout": {
          "type": "integer",
          "description": "time in ESCALATED before FAILED (default 60s)"
        }
      },
      "type": "object",
      "required": [
        "checkInterval",
        "silenceThreshold",
        "maxRetries",
        "retryBackoff",
        "cooldown",
        "escalationTimeout"
      ]
    },
    "WeatherSettings": {
      "properties": {
        "provider": {
          "type": "string",
          "description": "\"none\", \"yrno\", \"openweather\", or \"wunderground\""
        },
        "pollInterval": {
          "type": "integer",
          "description": "weather data polling interval in minutes"
        },
        "debug": {
          "type": "boolean",
          "description": "true to enable debug mode"
        },
        "openWeather": {
          "$ref": "#/$defs/OpenWeatherSettings",
          "description": "OpenWeather integration settings"
        },
        "wunderground": {
          "$ref": "#/$defs/WundergroundSettings",
          "description": "WeatherUnderground integration settings"
        }
      },
      "type": "object",
      "required": [
        "provider",
        "pollInterval",
        "debug",
        "openWeather",
        "wunderground"
      ]
    },
    "WebServerSettings": {
      "properties": {
        "debug": {
          "type": "boolean",
          "description": "true to enable debug mode"
        },
        "enabled": {
          "type": "boolean",
          "description": "true to enable web server"
        },
        "port": {
          "type": "string",
          "description": "port for web server"
        },
        "basePath": {
          "type": "string",
          "description": "reverse proxy subpath prefix (e.g., \"/birdnet\")"
        },
        "allowEmbedding": {
          "type": "boolean",
          "description": "true to allow embedding in iframes (e.g., Home Assistant)"
        },
        "liveStream": {
          "$ref": "#/$defs/LiveStreamSettings",
          "description": "live stream configuration"
        },
        "enableTerminal": {
          "type": "boolean",
          "description": "Enable browser terminal (security risk)"
        }
      },
      "type": "object",
      "required": [
        "debug",
        "enabled",
        "port",
        "basePath",
        "allowEmbedding",
        "liveStream",
        "enableTerminal"
      ]
    },
    "WebhookAuthConfig": {
      "properties": {
        "type": {
          "type": "string",
          "description": "\"none\", \"bearer\", \"basic\", \"custom\""
        },
        "token": {
          "type": "string",
          "description": "Token value or ${ENV_VAR}"
        },
        "token_file": {
          "type": "string",
          "description": "Path to file containing token"
        },
        "user": {
          "type": "string",
          "description": "Username value or ${ENV_VAR}"
        },
        "user_file": {
          "type": "string",
          "description": "Path to file containing username"
        },
        "pass": {
          "type": "string",
          "description": "Password value or ${ENV_VAR}"
        },
        "pass_file": {
          "type": "string",
          "description": "Path to file containing password"
        },
        "header": {
          "type": "string",
          "description": "Header name"
        },
        "value": {
          "type": "string",
          "description": "Header value or ${ENV_VAR}"
        },
        "value_file": {
          "type": "string",
          "description": "Path to file containing header value"
        }
      },
      "type": "object",
      "required": [
        "type",
        "token",
        "token_file",
        "user",
        "user_file",
        "pass",
        "pass_file",
        "header",
        "value",
        "value_file"
      ]
    },
    "WebhookEndpointConfig": {
      "properties": {
        "url": {
          "type": "string"
        },
        "method": {
          "type": "string",
          "description": "POST, PUT, PATCH (default: POST)"
        },
        "headers": {
          "additionalProperties": {
            "type": "string"
          },
          "type": "object",
          "description": "Custom HTTP headers"
        },
        "timeout": {
          "type": "integer",
          "description": "Per-endpoint timeout (default: use provider timeout)"
        },
        "auth": {
          "$ref": "#/$defs/WebhookAuthConfig",
          "description": "Authentication configuration"
        }
      },
      "type": "object",
      "required": [
        "url",
        "method",
        "headers",
        "timeout",
        "auth"
      ]
    },
    "WundergroundSettings": {
      "properties": {
        "apiKey": {
          "type": "string",
          "description": "WeatherUnderground API key"
        },
        "stationId": {
          "type": "string",
          "description": "WeatherUnderground station ID"
        },
        "endpoint": {
          "type": "string",
          "description": "WeatherUnderground API endpoint"
        },
        "units": {
          "type": "string",
          "description": "units of measurement: \"e\" (imperial), \"m\" (metric), \"h\" (UK hybrid)"
        }
      },
      "type": "object",
      "required": [
        "apiKey",
        "stationId",
        "endpoint",
        "units"
      ]
    },
    "YearlyTrackingSettings": {
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "true to enable yearly tracking"
        },
        "resetMonth": {
          "type": "integer",
          "description": "Month to reset yearly tracking (1=January, default: 1)"
        },
        "resetDay": {
          "type": "integer",
          "description": "Day to reset yearly tracking (default: 1)"
        },
        "windowDays": {
          "type": "integer",
          "description": "Days to show \"new this year\" indicator (default: 30)"
        }
      },
      "type": "object",
      "required": [
        "enabled",
        "resetMonth",
        "resetDay",
        "windowDays"
      ]
    }
  },
  "title": "BirdNET-Go Configuration",
  "description": "Configuration schema for BirdNET-Go's config.yaml file."
}
