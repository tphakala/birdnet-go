version: 2
updates:
  - package-ecosystem: gomod
    directory: /
    schedule:
      interval: weekly
    labels:
      - dependencies
      - go
    commit-message:
      prefix: "build(deps)"
    open-pull-requests-limit: 5
    ignore:
      # onnxruntime_go v1.31.0+ bundles the ORT API 26 header and requires a
      # native ONNX Runtime 1.26.x library. The toolchain pins native ONNX
      # Runtime 1.25.1 (Dockerfile, Taskfile.yml, setup-onnxruntime action),
      # so newer bindings fail InitializeEnvironment at runtime. Keep the
      # binding at v1.30.x until the native runtime is upgraded in lockstep.
      - dependency-name: "github.com/yalue/onnxruntime_go"
        versions: [">=1.31.0"]
    groups:
      go-minor-patch:
        patterns:
          - "*"
        update-types:
          - minor
          - patch

  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    labels:
      - dependencies
      - github-actions
    commit-message:
      prefix: "ci(deps)"
    open-pull-requests-limit: 5
    groups:
      actions:
        patterns:
          - "*"

  - package-ecosystem: npm
    directory: /frontend
    schedule:
      interval: weekly
    labels:
      - dependencies
      - frontend
    commit-message:
      prefix: "build(deps-frontend)"
    open-pull-requests-limit: 5
    # Ecosystem groups below include ALL update-types (major, minor, patch) so
    # tightly coupled toolchains upgrade in a single PR. This prevents the
    # situation where a plugin calls an API removed in a host package's major
    # release (e.g. eslint 9 -> 10 removing context.getSourceCode(), which
    # eslint-plugin-security@3 still called). Order matters: Dependabot assigns
    # each package to the FIRST matching group, so listing vite before svelte
    # keeps @sveltejs/vite-plugin-svelte travelling with vite majors (the
    # stronger coupling) rather than with svelte majors.
    groups:
      eslint:
        patterns:
          - "eslint"
          - "@eslint/*"
          - "eslint-plugin-*"
          - "eslint-config-*"
          - "@typescript-eslint/*"
          - "typescript-eslint"
          - "svelte-eslint-parser"
          - "@vitest/eslint-plugin"
      vite:
        # Listed before svelte so @sveltejs/vite-plugin-svelte lands here.
        # vite-plugin-svelte majors track vite majors (v6 needs vite ^6||^7,
        # v7 needs vite ^8), so this plugin must move with vite, not svelte.
        patterns:
          - "vite"
          - "vite-*"
          - "@sveltejs/vite-plugin-svelte"
      svelte:
        patterns:
          - "svelte"
          - "svelte-*"
          - "@sveltejs/*"
          - "prettier-plugin-svelte"
      vitest:
        patterns:
          - "vitest"
          - "vitest-*"
          - "@vitest/*"
      playwright:
        patterns:
          - "playwright"
          - "@playwright/*"
          - "@axe-core/playwright"
      tailwind:
        patterns:
          - "tailwindcss"
          - "@tailwindcss/*"
      testing-library:
        patterns:
          - "@testing-library/*"
      types:
        patterns:
          - "@types/*"
      npm-minor-patch:
        patterns:
          - "*"
        update-types:
          - minor
          - patch